apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-gateway.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "api-gateway.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
