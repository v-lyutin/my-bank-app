apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-prometheus
  labels:
    app: prometheus
spec:
  type: NodePort
  ports:
    - port: 9090
      targetPort: ui
      nodePort: 30090
      protocol: TCP
      name: ui
  selector:
    app: prometheus