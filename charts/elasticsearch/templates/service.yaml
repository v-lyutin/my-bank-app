apiVersion: v1
kind: Service
metadata:
  name: {{ include "elasticsearch.fullname" . }}
spec:
  selector:
    app: {{ include "elasticsearch.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 9200
      protocol: TCP