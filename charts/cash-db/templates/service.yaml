apiVersion: v1
kind: Service
metadata:
  name: {{ include "cash-db.fullname" . }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "cash-db.fullname" . }}
  ports:
    - name: postgres
      port: {{ .Values.service.port }}
      targetPort: 5432
