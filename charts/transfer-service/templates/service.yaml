apiVersion: v1
kind: Service
metadata:
  name: {{ include "transfer-service.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "transfer-service.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
